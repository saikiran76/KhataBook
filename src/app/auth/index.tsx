import { View, Text } from 'react-native'
import React from 'react'

const Authentication = () => {
  return (
    <View>
      {/* Design Auth Page */}
      <Text>Add Authentication Layer UI</Text>
    </View>
  )
}

export default Authentication